sort(nums.begin(), nums.end());
    for(int num: nums)
        cout << num << " ";
    cout << endl;

    sort(nums.rbegin(), nums.rend());
    for(int num: nums)
        cout << num << " ";
    cout << endl;